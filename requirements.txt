# Django
django

# RAG dependencies (same as your existing project)
sentence-transformers
faiss-cpu
firecrawl-py
gitpython
pymupdf          # PDF text extraction (imported as fitz)

# Optional: production server
gunicorn
